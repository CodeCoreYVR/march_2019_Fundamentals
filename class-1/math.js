/**
 * Through the Math object
 * I'm going to generate
 * a random number.
 * 
 * "Math" is the name 
 * of the object. random
 * is the name of the
 * function.
 * 
 * parentesis tells the
 * program that we want to
 * execute the random
 * function.
 * 
 * the semi colon, is desined
 * to end an instruction.
 */
Math.random();

/**
 * console is a built in object
 * log is a function within console object
 * which receives a parameter. This parameter
 * will be printed out to the console.
 */

console.log( Math.floor(34.40) );
console.log( Math.ceil(34.60) );

/**
 * Write a piece of code that generatesw
 * a random number between 0 and 99.
 */
Math.floor( Math.random() * 100 );

/**
 * How to solve problems?
 * 
 * Split the bigger challenge into
 * smaller tasks and take it step by
 * step.
 */

